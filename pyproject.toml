[project]
name = "ads"
version = "0.1.0"
description = "Autonomous Driving Systems assignments"
requires-python = ">=3.10, <3.12"
dependencies = [
    "numpy",
    "matplotlib",
    "opencv-python",
    "gtsam",
    "open3d",
    "tqdm",
    "jupyter",
    "casadi",
]

[tool.uv]
package = false